CREATE TABLE students (
	nim varchar(128) NOT NULL,
	id_admin INT NOT NULL,
	first_name varchar(128) NOT NULL,
	last_name varchar(128) NOT NULL,
	selected_courses varchar(128) NOT NULL,
	status varchar(128),
	PRIMARY KEY (nim)
);




